# Python 3.13 compatible — all packages have pre-built cp313 wheels.
#
# Pin notes:
#   mlflow 2.18.0 is the first 2.x release that allows pyarrow >= 18;
#     earlier 2.x required pyarrow<18, which has no cp313 wheel.
#   evidently is capped at the 0.4.x series; 0.5+ rewrote the import
#     paths used in src/monitoring/drift_detection.py (breaking change).

# Core ML
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.8.0
imbalanced-learn==0.14.1

# MLflow  (2.18 first to allow pyarrow >= 18)
mlflow==2.18.0
boto3==1.43.16

# Serving
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
python-multipart==0.0.29

# Monitoring (0.7.21 is first version supporting numpy >= 2.1 / Python 3.13)
# [ui] extra adds the local dashboard server (evidently ui --workspace ...)
evidently[ui]==0.7.21

# Testing
pytest==9.0.3
pytest-cov==7.1.0
httpx==0.28.1
moto[s3]==5.2.1        # mock AWS S3 for feature store tests

# Utilities
python-dotenv==1.2.2
joblib==1.5.3
pyarrow==18.1.0         # Parquet I/O for offline feature store
