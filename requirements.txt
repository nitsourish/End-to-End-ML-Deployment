# Python 3.13 compatible — all packages have pre-built cp313 wheels.
#
# Pin notes:
#   mlflow 3.1.4 matches the version auto-installed on the EC2 tracking
#     server (pip install mlflow pulls latest 3.x). Keeping client and
#     server on the same major version avoids REST API edge-case differences.
#   evidently 0.7.21 first version supporting numpy >= 2.1 / Python 3.13.

# Core ML
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.8.0
imbalanced-learn==0.14.1

# MLflow  (3.1.4 matches EC2 tracking server version)
mlflow==3.1.4
boto3==1.43.16

# Serving
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
python-multipart==0.0.29

# Monitoring (0.7.21 is first version supporting numpy >= 2.1 / Python 3.13)
# UI server (evidently ui --workspace ...) is included in the base package —
# there is no separate [ui] extra in 0.7.21.
evidently==0.7.21
# evidently 0.7.21 requires litestar>=2.19.0 but crashes on 2.21+ due to
# stricter kwarg-ambiguity validation in litestar's route registration
# (ImproperlyConfiguredException on the /search/{project_name:str} route).
# Pin to 2.19.0 until evidently ships a fix.
litestar==2.19.0

# Testing
pytest==9.0.3
pytest-cov==7.1.0
httpx==0.28.1
moto[s3]==5.2.1        # mock AWS S3 for feature store tests

# Utilities
python-dotenv==1.2.2
joblib==1.5.3
pyarrow==18.1.0         # Parquet I/O for offline feature store
